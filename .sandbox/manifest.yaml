hooks:
  build:
    cmd: ./sandbox.sh

daemons:
  app:
    run:
      cmd: PATH=$HOME/.rbenv/shims:"$PATH" rails s -b 0.0.0.0 -p 3001
  sidekiq:
    run:
      cmd: PATH=$HOME/.rbenv/shims:"$PATH" bundle exec sidekiq
